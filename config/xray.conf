# ============================================================
# SERVER 1: WS (Vless, Vmess, Trojan, SS) - Port 1010
# ============================================================
server {
    listen 1010 proxy_protocol so_keepalive=on reuseport;
    server_name xxx;
    
    # --- Fix IP dari HAProxy ---
    set_real_ip_from 127.0.0.1;
    real_ip_header proxy_protocol;
    
    # --- Buffer Settings ---
    client_body_buffer_size 200K;
    client_header_buffer_size 2k;
    client_max_body_size 10M;
    large_client_header_buffers 3 1k;
    
    # --- Timeout yang Masuk Akal (1 Hari) ---
    client_header_timeout 1d;
    client_body_timeout 1d;
    keepalive_timeout 1d;
    
    # --- VLESS WS ---
    location /vless {
        if ($http_upgrade != "websocket") {
            return 404;
        }
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        # Menggunakan $remote_addr karena sudah di-set oleh real_ip_header
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # --- VMESS WS ---
    location /vmess {
        if ($http_upgrade != "websocket") {
            return 404;
        }
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # --- TROJAN WS ---
    location /trojan-ws {
        if ($http_upgrade != "websocket") {
            return 404;
        }
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # --- SHADOWSOCKS WS ---
    location /ss-ws {
        if ($http_upgrade != "websocket") {
            return 404;
        }
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # --- FALLBACK / SSH WS ---
    location / {
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10015;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# ============================================================
# SERVER 2: OVPN / Fallback Lain - Port 1012
# ============================================================
server {
    listen 1012 proxy_protocol so_keepalive=on reuseport;
    server_name xxx;

    # Fix IP HAProxy (Penting!)
    set_real_ip_from 127.0.0.1;
    real_ip_header proxy_protocol;

    client_header_timeout 1d;
    keepalive_timeout 1d;

    location / {
        proxy_redirect off;
        proxy_pass http://127.0.0.1:10012;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# ============================================================
# SERVER 3: SSL Fallback / Webroot - Port 81
# ============================================================
server {
    listen 81 ssl http2 reuseport;
    server_name xxx;
    
    ssl_certificate /etc/xray/xray.crt;
    ssl_certificate_key /etc/xray/xray.key;
    ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+ECDSA+AES128:EECDH+aRSA+AES128:RSA+AES128:EECDH+ECDSA+AES256:EECDH+aRSA+AES256:RSA+AES256:EECDH+ECDSA+3DES:EECDH+aRSA+3DES:RSA+3DES:!MD5;
    ssl_protocols TLSv1.2 TLSv1.3;
    
    root /var/www/html;
    index index.html;
}

# ============================================================
# SERVER 4: gRPC - Port 1013
# ============================================================
server {
    listen 1013 http2 proxy_protocol so_keepalive=on reuseport;
    server_name xxx;

    # Fix IP HAProxy (Penting untuk gRPC juga)
    set_real_ip_from 127.0.0.1;
    real_ip_header proxy_protocol;

    client_header_timeout 1d;
    keepalive_timeout 1d;

    # VLESS gRPC
    location /vless-grpc {
        grpc_pass grpc://127.0.0.1:10005;
        grpc_set_header Host $host;
        grpc_set_header X-Real-IP $remote_addr;
        grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # VMESS gRPC
    location /vmess-grpc {
        grpc_pass grpc://127.0.0.1:10006;
        grpc_set_header Host $host;
        grpc_set_header X-Real-IP $remote_addr;
        grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # TROJAN gRPC
    location /trojan-grpc {
        grpc_pass grpc://127.0.0.1:10007;
        grpc_set_header Host $host;
        grpc_set_header X-Real-IP $remote_addr;
        grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # SS gRPC
    location /ss-grpc {
        grpc_pass grpc://127.0.0.1:10008;
        grpc_set_header Host $host;
        grpc_set_header X-Real-IP $remote_addr;
        grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
